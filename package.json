{
  "name": "purpose-investor-network-backend",
  "version": "0.1.0",
  "private": true,
  "main": "backend/index.js",
  "scripts": {
    "start": "node backend/index.js",
    "test": "jest",
    "package:backend": "mkdir -p dist && rm -f dist/backend.zip && cd backend && zip -rq ../dist/backend.zip . ../package.json ../package-lock.json ../node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.379.0",
    "@aws-sdk/client-s3": "^3.379.0",
    "@aws-sdk/lib-dynamodb": "^3.379.0",
    "@sentry/node": "^8.55.2",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-session": "^1.17.4",
    "serverless-http": "^3.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}