# Purpose Investor Network — Project Standards

Full project standards now live at [`docs/CLAUDE.md`](../docs/CLAUDE.md), alongside
`ARCHITECTURE.md` and `DATA-MODEL.md`. This stub stays here because Claude Code only
auto-loads project instructions from this exact path (`.claude/CLAUDE.md`) at session
start — without it, a new session gets no automatic pointer to the real standards doc.

@docs/CLAUDE.md
